{
  "name": "atlax-langfuse-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "bun-types": "1.3.13",
    "typescript": "^5.4.0",
    "zod": "^3.24.0"
  }
}
